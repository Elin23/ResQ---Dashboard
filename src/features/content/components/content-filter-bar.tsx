import{Button,FilterBar,Input,Select}from'@/components/ui';import{contentStatusOptions,articleCategoryOptions,commonTags}from'../constants';import type{ContentListFilters}from'../types';export function ContentFilterBar({filters,onChange,articles=false}:{filters:ContentListFilters;onChange:(p:Partial<ContentListFilters>)=>void;articles?:boolean}){const active=Boolean(filters.search||filters.status||filters.category||filters.author||filters.tag||filters.dateFrom||filters.dateTo);return<FilterBar><Input aria-label="بحث" placeholder="بحث بالعنوان أو الرقم أو الرابط المختصر" value={filters.search} onChange={e=>onChange({search:e.target.value,page:1})} className="sm:max-w-xs"/><Select value={filters.status??'ALL'} onValueChange={v=>onChange({status:v==='ALL'?undefined:v as ContentListFilters['status'],page:1})} options={[{value:'ALL',label:'كل الحالات'},...contentStatusOptions]}/>{articles&&<><Select value={filters.category??'ALL'} onValueChange={v=>onChange({category:v==='ALL'?undefined:v,page:1})} options={[{value:'ALL',label:'كل التصنيفات'},...articleCategoryOptions]}/><Select value={filters.tag??'ALL'} onValueChange={v=>onChange({tag:v==='ALL'?undefined:v,page:1})} options={[{value:'ALL',label:'كل الوسوم'},...commonTags.map(value=>({value,label:value}))]}/></>}<Input aria-label="من تاريخ" type="date" value={filters.dateFrom??''} onChange={e=>onChange({dateFrom:e.target.value||undefined,page:1})} className="sm:w-auto"/><Input aria-label="إلى تاريخ" type="date" value={filters.dateTo??''} onChange={e=>onChange({dateTo:e.target.value||undefined,page:1})} className="sm:w-auto"/>{active&&<Button variant="ghost" onClick={()=>onChange({search:'',status:undefined,category:undefined,author:undefined,tag:undefined,dateFrom:undefined,dateTo:undefined,page:1})}>مسح الفلاتر</Button>}</FilterBar>}
