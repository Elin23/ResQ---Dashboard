import { Badge } from '@/components/ui';
import type { MapEntity } from '../types';
import { entityTypeLabels } from '../constants';
export function MapEntityList({entities,selectedId,onSelect}:{entities:MapEntity[];selectedId?:string;onSelect:(entity:MapEntity)=>void}){return <div className="max-h-[22rem] space-y-1.5 overflow-y-auto pe-1" aria-label="الأماكن الظاهرة على الخريطة">{entities.map(entity=><button key={entity.id} type="button" onClick={()=>onSelect(entity)} className={`w-full rounded-lg border px-3 py-2.5 text-start transition-colors ${selectedId===entity.id?'border-primary/25 bg-primary/[0.05]':'border-border/35 hover:bg-primary/[0.025]'}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-[12px] font-medium text-foreground">{entity.title}</p><p className="mt-1 truncate text-[11px] text-muted-foreground">{entity.governorate}{entity.city?` — ${entity.city}`:''} · {entity.address}</p></div><Badge tone="neutral">{entityTypeLabels[entity.type]}</Badge></div></button>)}</div>}
