import { deepLinkPatterns,notificationAudienceTypeLabels } from '../constants';import type { NotificationAudience,NotificationChannel } from '../types';import{safeFormatDate}from'@/lib/runtime-safety';
export function isValidNotificationDeepLink(value:string|undefined):boolean{if(!value)return true;if(value.startsWith('javascript:')||value.startsWith('data:')||value.startsWith('http:')||value.startsWith('https:'))return false;return deepLinkPatterns.some((item)=>item.pattern.test(value));}
export function summarizeAudience(audience:NotificationAudience):string{if(!audience)return'جمهور مخصص';if(audience.everyone)return 'جميع مستخدمي ResQ';const parts:string[]=[];if(audience.userTypes?.length)parts.push(audience.userTypes.map((t)=>notificationAudienceTypeLabels[t]??'نوع مستخدم').join('، '));if(audience.governorates?.length)parts.push(`في ${audience.governorates.join('، ')}`);if(audience.organizationIds?.length)parts.push(`${audience.organizationIds.length} جمعيات محددة`);if(audience.userIds?.length)parts.push(`${audience.userIds.length} مستخدمين محددين`);return parts.join(' · ')||'جمهور مخصص';}
export function formatChannels(channels:NotificationChannel[]):string{return (Array.isArray(channels)?channels:[]).map((c)=>c==='IN_APP'?'داخل التطبيق':'Push').join(' + ')||'—';}
export function formatDateTime(value?:string):string{return safeFormatDate(value,{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Damascus'});}
