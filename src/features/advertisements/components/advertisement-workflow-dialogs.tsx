import { useState } from 'react';
import { toast } from 'sonner';
import { Button, Modal, Textarea } from '@/components/ui';
import { useDeleteAdvertisement, usePauseAdvertisement } from '../hooks';
export function PauseAdvertisementDialog({ id, open, onOpenChange }: { id: string; open: boolean; onOpenChange: (value: boolean) => void }) { const [reason, setReason] = useState(''); const mutation = usePauseAdvertisement(id); return <Modal open={open} onOpenChange={onOpenChange} title="إيقاف الإعلان" description="يبقى الإعلان محفوظًا ويمكن إعادة نشره لاحقًا." footer={<><Button variant="secondary" className="h-9 rounded-xl" onClick={() => onOpenChange(false)}>إلغاء</Button><Button className="h-9 rounded-xl" disabled={reason.trim().length < 3 || mutation.isPending} onClick={() => mutation.mutate(reason.trim(), { onSuccess: () => { toast.success('تم إيقاف الإعلان'); onOpenChange(false); }, onError: () => toast.error('تعذر إيقاف الإعلان') })}>إيقاف</Button></>}><label className="text-[12px] font-medium">سبب الإيقاف<Textarea className="mt-1.5 rounded-xl" value={reason} onChange={(e) => setReason(e.target.value)} /></label></Modal>; }
export function DeleteAdvertisementDialog({ id, open, onOpenChange, onDeleted }: { id: string; open: boolean; onOpenChange: (value: boolean) => void; onDeleted?: () => void }) { const [reason, setReason] = useState(''); const mutation = useDeleteAdvertisement(id); return <Modal open={open} onOpenChange={onOpenChange} title="حذف الإعلان" description="سيختفي الإعلان من قائمة الإدارة ومن النشر." footer={<><Button variant="secondary" className="h-9 rounded-xl" onClick={() => onOpenChange(false)}>إلغاء</Button><Button variant="danger" className="h-9 rounded-xl" disabled={reason.trim().length < 3 || mutation.isPending} onClick={() => mutation.mutate(reason.trim(), { onSuccess: () => { toast.success('تم حذف الإعلان'); onOpenChange(false); onDeleted?.(); }, onError: () => toast.error('تعذر حذف الإعلان') })}>حذف</Button></>}><label className="text-[12px] font-medium">سبب الحذف<Textarea className="mt-1.5 rounded-xl" value={reason} onChange={(e) => setReason(e.target.value)} /></label></Modal>; }
