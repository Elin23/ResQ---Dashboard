import{MetricCard,Skeleton}from'@/components/ui';import type{SupportSummary}from'../types';
const cards=[['newCount','طلبات جديدة','NEW'],['openCount','مفتوحة','OPEN'],['urgentCount','عاجلة','URGENT'],['waitingForUser','بانتظار المستخدم','WAITING_FOR_USER'],['unassigned','غير مسندة','UNASSIGNED'],['resolvedToday','تم حلها اليوم','RESOLVED']] as const;
export function SupportSummaryCards({summary,loading,onFilter}:{summary?:SupportSummary;loading:boolean;onFilter:(key:string)=>void}){return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6">{cards.map(([key,label,filter])=>loading?<div key={key}><Skeleton className="h-24"/></div>:<button key={key} type="button" className="text-start" onClick={()=>onFilter(filter)}><MetricCard label={label} value={summary?.[key]??0}/></button>)}</div>}
