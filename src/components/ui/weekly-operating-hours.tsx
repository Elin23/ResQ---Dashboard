export interface WeeklyHoursRow { day:string; closed:boolean; open24Hours?:boolean; opensAt?:string; closesAt?:string; }
export function WeeklyOperatingHours({hours,dayLabels}:{hours:WeeklyHoursRow[];dayLabels:Record<string,string>}){return <div className="grid gap-2 sm:grid-cols-2">{hours.map(h=><div key={h.day} className="flex justify-between rounded-md bg-muted/60 px-3 py-2 text-sm"><span>{dayLabels[h.day]??h.day}</span><span className="font-semibold">{h.open24Hours?'24 ساعة':h.closed?'مغلق':`${h.opensAt??'—'} — ${h.closesAt??'—'}`}</span></div>)}</div>}
